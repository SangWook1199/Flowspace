spring:
  application:
    name: flowspace
  datasource:
    url: jdbc:mysql://localhost:3306/flowspace?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: P@ssw0rd
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    time-zone: Asia/Seoul

server:
  port: 8080
