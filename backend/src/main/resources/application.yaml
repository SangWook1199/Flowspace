spring:
  application:
    name: flowspace

  profiles:
    active: secret

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    time-zone: Asia/Seoul

server:
  port: 8080
